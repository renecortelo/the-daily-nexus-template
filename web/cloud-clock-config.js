// This committed default deliberately has no deployment-specific endpoint.
// Private publishing replaces it at release time from an encrypted setting.
window.TDN_CLOUD_CLOCK = Object.freeze({ endpoint: "" });
